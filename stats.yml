name: Stats

on:
push:
  branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-lates

    steps:
    - name: Checkout repo
      uses: actions/checkout@v4
    
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Run script
      run: python ./Roadmap/stats.py

    - nme: Commit and Push changes
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: Update stats
        commit_user_name: samuelR25 [GitHub Actions]
        commit_user_email: samuelrincones31@gmail.com
        commit_author: Samuel JR
        
